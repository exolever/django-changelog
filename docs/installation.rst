============
Installation
============

At the command line::

    $ easy_install exo-changelog

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv exo-changelog
    $ pip install exo-changelog
